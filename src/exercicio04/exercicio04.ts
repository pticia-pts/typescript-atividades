
export function array(numeros:number[]):number[]{
// o teste espera os dois primeiros elementos.
// indice inicial = 0 (primeiro)
// indice final= 2 (n incluido)
// resultado = [2,4]
    const remove= numeros.slice(0,2);

    return remove
}